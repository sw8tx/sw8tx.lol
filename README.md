# sw8tx.lol

Production portfolio site for Sparkle / Tyler Osthoff.

## Scripts

```bash
npm run dev
npm run lint
npm run build
```

## Structure

- `app/` routes and app shell
- `components/` shared React components
- `public/` production assets
- `styles/` global styles
